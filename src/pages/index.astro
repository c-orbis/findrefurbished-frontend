---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Vind Refurbished - Alle aanbiedingen">
  <main class="max-w-7xl mx-auto px-4 py-8">
    <h1 class="text-4xl font-black mb-8 text-gray-900 leading-tight">Vind Refurbished</h1>
    
    <!-- Zoekbalk bovenaan -->
    <div id="searchbox" class="mb-8"></div>

    <div class="flex flex-col md:flex-row gap-10">
      <!-- Sidebar Filters (Links) -->
      <aside class="w-full md:w-72 space-y-8">
        <div class="filter-section border-b pb-6 border-gray-100">
          <h3 class="font-bold text-gray-400 uppercase text-[10px] tracking-widest mb-4 italic">Categorie</h3>
          <div id="category-filter"></div>
        </div>
        <div class="filter-section border-b pb-6 border-gray-100">
          <h3 class="font-bold text-gray-400 uppercase text-[10px] tracking-widest mb-4 italic">Merk</h3>
          <div id="brand-filter"></div>
        </div>
        <div class="filter-section">
          <h3 class="font-bold text-gray-400 uppercase text-[10px] tracking-widest mb-4 italic">Conditie</h3>
          <div id="condition-filter"></div>
        </div>
      </aside>

      <!-- Resultaten Lijst (Rechts) -->
      <div class="flex-grow">
        <div id="hits"></div>
      </div>
    </div>
  </main>

  <!-- Data element voor de keys vanuit Vercel naar JS -->
  <div id="meili-data" 
       data-host={import.meta.env.PUBLIC_MEILI_HOST} 
       data-key={import.meta.env.PUBLIC_MEILI_SEARCH_KEY}>
  </div>
</Layout>

<script>
  import { instantMeiliSearch } from '@meilisearch/instant-meilisearch';
  import instantsearch from 'instantsearch.js';
  import { searchBox, hits, refinementList } from 'instantsearch.js/es/widgets';

  function init() {
    const dataEl = document.getElementById('meili-data');
    if (!dataEl || !dataEl.dataset.host) return;

    const { host, key } = dataEl.dataset;

    // FIX: We forceren de client creatie met een duidelijke variabele
    const meiliSearchClient = instantMeiliSearch(host, key);

    const search = instantsearch({
      indexName: 'products',
      // Expliciete toewijzing om de "search method" error te voorkomen
      searchClient: meiliSearchClient,
    });

    search.addWidgets([
      searchBox({ 
        container: '#searchbox', 
        placeholder: 'Zoek op model (bijv. iPhone 15 Pro)...' 
      }),
      
      refinementList({ container: '#category-filter', attribute: 'category_primary' }),
      refinementList({ container: '#brand-filter', attribute: 'brand' }),
      refinementList({ container: '#condition-filter', attribute: 'offers.condition' }),

      hits({
        container: '#hits',
        templates: {
          empty: (results) => `<p class="p-10 text-center text-gray-500 bg-white rounded-xl border border-dashed border-gray-300 italic">Geen producten gevonden voor "${results.query}".</p>`,
          item: (hit) => `
            <div class="flex items-center gap-6 bg-white border border-gray-200 p-5 rounded-2xl mb-4 shadow-sm hover:border-blue-500 transition-all hover:shadow-md group">
              <div class="w-24 h-24 flex-shrink-0 flex items-center justify-center p-2 bg-gray-50 rounded-xl group-hover:bg-white transition-colors">
                <img src="https://img.findrefurbished.com/${hit.master_id}.png" class="max-h-full transition-transform group-hover:scale-105" alt="${hit.title_h1}" onerror="this.src='https://placehold.com'" />
              </div>
              <div class="flex-grow">
                <h2 class="text-xl font-bold text-gray-900 group-hover:text-blue-600 transition-colors">${hit.title_h1}</h2>
                <p class="text-gray-400 text-[11px] uppercase font-bold tracking-widest mt-1">${hit.brand} • ${hit.category_primary}</p>
              </div>
              <div class="text-right flex flex-col justify-center min-w-[130px]">
                <div class="text-[10px] text-gray-400 font-bold uppercase tracking-tighter mb-[-4px]">Vanaf</div>
                <div class="text-3xl font-black text-blue-600">€${hit.min_price.toFixed(2)}</div>
                <a href="/product/${hit.variant_slug}" class="inline-block mt-3 bg-blue-600 text-white px-6 py-2 rounded-xl font-bold hover:bg-blue-800 transition-colors text-center text-sm shadow-sm">Bekijk Deals</a>
              </div>
            </div>
          `,
        },
      }),
    ]);

    search.start();
    console.log("Zoekmachine succesvol gestart op " + host);
  }

  // Zorg dat Astro de JS laadt bij elke pagina-navigatie en eerste load
  document.addEventListener('astro:page-load', init);
  init();
</script>
